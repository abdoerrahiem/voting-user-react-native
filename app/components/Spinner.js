import React from 'react'
import {ActivityIndicator} from 'react-native'
import {colors} from '../utils'

const Spinner = () => <ActivityIndicator size="large" color={colors.orange} />

export default Spinner
